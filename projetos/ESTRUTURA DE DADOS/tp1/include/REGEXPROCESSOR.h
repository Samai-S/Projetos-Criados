#include <string>

using namespace std;

string verifica_host(string);//retorna host da string bruta

bool verifica_URL(string);//Verifica se URL é valida

int processa_niveis(string);//processa os niveis das strings